extends layout

block content
    h1= title 
    p   Do you want to delete this copy
    form(action="", method="post")
        input.form-control(type="hidden" value=bookinstance._id name="bookinstance_id" required=true)
        button.btn.btn-primary(type="submit") Delete