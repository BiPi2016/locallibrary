extends layout

block content
    h1 #{title}

    form(action="", method="post")
        .form-group
            label(for="first_name") First Name:
            input#first_name.form-control(type="text" placeholder="First Name" name="first_name" required='true' value=(author === undefined ? '' : author.first_name))
            label(for="last_name") Last Name:
            input#last_name.form-control(type="text" placeholder='Last Name' name="last_name" required='true' value=(author === undefined ? '' : author.last_name))
        .form-group
            label(for="date_of_birth") Date of birth:
            input#date_of_birth.form-control(type="date" name="date_of_birth" placeholder='dd/mm/yyyy' value=( author === undefined ? '': author.date_of_birth))
            label(for="date_of_death") Date of death:
            input#date_of_death.form-control(type="date" name="date_of_death" placeholder='dd/mm/yyyy' value=( author === undefined ? '': author.death_of_death))
        button.btn.btn-primary(type='submit') Submit
        if errors
            ul
                for error in errors 
                    li!= error.msg   